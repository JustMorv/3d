
# Редактор кухни
Профессиональный 3D редактор для проектирования кухонь с поддержкой загрузки моделей, реалистичными столкновениями и интуитивным управлением.

# 📁 Структура проекта

```html
pricer/
├── dist/                    # Библиотека three-dragger
│   ├── three-dragger.esm.js
│   ├── three-dragger.umd.js
│   └── ...
├── models/                  # 3D модели кухни
│   └── kitchen/
│       ├── basic_kitchen_cabinets_and_counter.glb
│       ├── cabinet.glb
│       ├── kitchen.glb
│       ├── kitchen_cabinets.glb
│       └── ... (15+ моделей)
└── example/                 # Веб-приложение
    ├── textures/            # Текстуры стен и пола
    │   ├── floor.jpg
    │   └── wall.jpg
    ├── modules/             # Исходный код приложения
    │   ├── Core/            # Ядро приложения
    │   │   ├── App.js
    │   │   ├── SceneManager.js
    │   │   ├── CameraController.js
    │   │   └── UIManager.js
    │   ├── Models/          # Работа с моделями
    │   │   ├── ModelLoader.js
    │   │   ├── ModelManager.js
    │   │   └── ModelNormalizer.js
    │   ├── Interactions/    # Взаимодействия
    │   │   ├── DragManager.js
    │   │   ├── SelectionManager.js
    │   │   ├── RotationManager.js
    │   │   └── ContextMenu.js
    │   ├── Physics/         # Физика и столкновения
    │   │   ├── CollisionManager.js
    │   │   ├── CollisionHelper.js
    │   │   └── WallManager.js
    │   └── Utils/           # Вспомогательные функции
    │       ├── MathHelper.js
    │       ├── DebugHelper.js
    │       └── EventManager.js
    ├── index.html           # Главная HTML страница
    ├── styles.css           # Стили приложения
    └── main.js              # Точка входа приложения
```

# Быстрый старт
1. Установка и запуск
2. Скопируйте проект в удобную директорию
3. Запустите локальный сервер одним из способов:


# Python 3
```python
cd C:\Users\nsmorozov\Desktop\pricer\example
python -m http.server 8000
```

# Live Server (VS Code)

1. Установите расширение Live Server
2. ПКМ по index.html
3. Open with Live Server


## 📂 Настройка пути к моделям

Если сервер запущен из папки `example`, пути к моделям **уже настроены корректно**.

Все 3D-модели находятся в каталоге: ``../models/kitchen/.``


Дополнительной настройки не требуется.

---

##  Как пользоваться

###  Основное меню

Основное меню расположено **слева вверху** интерфейса.  
Оно состоит из **трёх основных разделов**.

---

###  3D Модели кухни

Раздел предназначен для загрузки различных кухонных моделей в сцену.

Доступные модели:

- **Базовая кухня** — `basic_kitchen_cabinets_and_counter.glb`
- **Кухонный шкаф** — `cabinet.glb`
- **Кухня** — `kitchen.glb`
- **Кухня 1** — `kitchen1.glb`
- **Кухонные шкафы** — `kitchen_cabinets.glb`
- **Современная кухня** — `modern_kitchen.glb`
- **И другие** — всего доступно **15+ моделей**

После выбора модель автоматически добавляется в 3D-сцену.

---

###  Управление объектами

В этом разделе находятся инструменты для работы с объектами на сцене.

| Кнопка | Функция | Описание |
|------|--------|---------|
| 🗑️ | `deleteSelected()` | Удаляет выделенный объект |
| 🔥 | `clearAllModels()` | Очищает сцену от всех моделей |
| 🧱 | `snapToWalls()` | Автоматически прижимает объекты к стенам |
| 📏 | `enableWidthMode()` | Включает режим изменения ширины объекта |
| 🔄 | `resetObject()` | Возвращает объект в исходное состояние |
| ⬆️ | `alignToFloor()` | Выровнять объект по уровню пола |
| ⬆️⬆️ | `fixAllModelsHeight()` | Корректирует высоту всех объектов |
| 📊 | `checkAllModelSizes()` | Выводит размеры моделей в консоль |
| ⚖️ | `normalizeAllModels()` | Приводит все модели к единому размеру |
| 🎯 | `recenterAllModels()` | Центрирует все модели в комнате |
| 🔄 | `resetAllPositions()` | Возвращает объекты в границы комнаты |

---

### 🎯 Коллизии

Раздел управления системой столкновений объектов.

Позволяет:
- предотвращать наложение моделей
- автоматически учитывать стены и другие объекты
- визуально отлаживать границы столкновений


### 🎯 Управление коллизиями

В данном разделе настраивается система столкновений объектов.

Доступные опции:

- **Точные столкновения** — включает или выключает сложную систему коллизий  
- **Показать коллайдеры** — визуализирует границы столкновений объектов  
- **Скрыть коллайдеры** — скрывает отладочную информацию коллизий  

Коллизии работают в реальном времени и предотвращают наложение объектов друг на друга.

---

## 🎬 Управление сценой

### 📷 Камера

Управление камерой осуществляется стандартными средствами **Three.js (OrbitControls)**:

- **ЛКМ + движение мыши** — вращение камеры вокруг сцены  
- **ПКМ + движение мыши** — панорамирование (смещение камеры)  
- **Колёсико мыши** — приближение и отдаление камеры  

Камера всегда ориентируется на центр сцены.

---

## 🖱️ Работа с объектами

### 🔍 Выбор объекта

Для выбора объекта:

1. Кликните **ЛКМ** по модели  
2. Вокруг выбранного объекта появится подсветка  
3. Появится иконка ⚙️ для вызова контекстного меню  

---

### 🚚 Перемещение объекта

1. Выберите объект  
2. Зажмите **ЛКМ** и перемещайте мышь  

Во время перемещения работает система коллизий:

- 🟢 **Зелёный цвет** — перемещение разрешено  
- 🔴 **Красный цвет** — обнаружено столкновение  

Наложение объектов автоматически предотвращается.

---

### ⚙️ Контекстное меню (шестерёнка)

Контекстное меню появляется при выборе объекта и предоставляет следующие действия:

- ➕ **Увеличить на 20%** — масштабирование вверх  
- ➖ **Уменьшить на 20%** — масштабирование вниз  
- 🔄 **Повернуть объект** — включает режим вращения  
- ↩️ **Сбросить вращение** — возвращает исходную ориентацию  
- ⎘ **Дублировать** — создаёт копию объекта  
- 🗑️ **Удалить объект** — удаляет выбранный объект  

---

### 🔄 Вращение объекта

Для вращения объекта:

1. Откройте контекстное меню ⚙️  
2. Выберите пункт **«Повернуть объект»**  
3. На объекте появится круг с ручкой вращения  
4. Перетаскивайте ручку для изменения ориентации  

Для возврата к исходному положению используйте **«Сбросить вращение»**.

---

### 📏 Изменение ширины объекта

1. Выберите объект  
2. Нажмите **«Изменить ширину»** в основном меню  
3. Появится панель с ползунком **от 50% до 200%**  
4. Используйте пресеты или точную настройку  

Чтобы выйти из режима:
- закройте панель  
- или нажмите **Escape**

Изменения применяются в реальном времени.


## 🔧 Технические особенности

### 🎯 Система столкновений

Система коллизий поддерживает **два режима работы**:

- **Простой режим** — быстрая проверка столкновений по *bounding box*
- **Точный режим** — сложная система столкновений с использованием коллайдеров

#### Параметры коллизий

- `collisionMargin = 15` — минимальное расстояние между объектами
- `wallOffsetMargin = 15` — отступ объектов от стен
- `wallSnapDistance = 20` — расстояние для автоматической пристыковки к стенам

---

### 📐 Масштабирование моделей

В проекте используется **автоматическая нормализация размеров моделей**.

Основные правила:

- Все модели приводятся к **целевой ширине — 300 единиц**
- **Исключение:** гостиная — **150 единиц**
- Максимальная высота модели ограничена **250 единиц**
- Пропорции оригинальных моделей сохраняются

#### Формула масштабирования

```js
widthScale = TARGET_WIDTH / originalWidth
scaledHeight = originalHeight * widthScale
heightScale = (scaledHeight > MAX_HEIGHT)
  ? MAX_HEIGHT / scaledHeight
  : 1
finalScale = widthScale * heightScale * TARGET_HEIGHT_SCALE
```

## 🏠 Физические параметры комнаты

Рабочая сцена имеет фиксированные физические размеры, в пределах которых размещаются все объекты.

- **Ширина (X):** 450 единиц
- **Глубина (Z):** 450 единиц
- **Высота (Y):** 270 единиц
- **Уровень пола:** 0 единиц

Все объекты автоматически ограничиваются границами комнаты.

---

## 🐛 Отладка и диагностика

### 🧪 Консоль браузера (F12)

Для диагностики и анализа состояния сцены используйте консоль разработчика.

Полезные команды:

```js
// Проверить размеры всех моделей
window.app.modelManager.checkAllModelSizes()

// Посмотреть количество объектов на сцене
console.log(
  'Объектов на сцене:',
  window.app.sceneManager.objects.length
)

// Получить информацию о выбранном объекте
if (window.app.selectionManager.selectedObject) {
  console.log(
    'Выбранный объект:',
    window.app.selectionManager.selectedObject.userData
  )
}

// Управление режимом отладки коллизий
window.app.collisionManager.showColliders()
window.app.collisionManager.hideColliders()
```

### 🧩 Дебаг-режим

В проекте предусмотрен расширенный режим отладки, предназначенный для диагностики и анализа работы сцены.

Доступные возможности:

- **Визуализация коллайдеров** — отображение границ столкновений в виде красных *wireframe*-боксов
- **Статистика** — отображение FPS и количества объектов на сцене
- **Логирование** — вывод подробной информации о работе системы в консоль браузера

Дебаг-режим используется для поиска ошибок и оптимизации производительности.

---

## 📋 Поддерживаемые форматы моделей

Проект поддерживает загрузку следующих форматов 3D-моделей:

- **GLTF / GLB** *(рекомендуется)* — загрузка через `GLTFLoader`
- **OBJ** — загрузка через `OBJLoader`

---

### 📦 Требования к моделям

При подготовке и добавлении моделей рекомендуется соблюдать следующие требования:

- **Размер файла:** оптимально до 50 MB
- **Текстуры:** встроенные в модель или подключаемые отдельно
- **Единицы измерения:** реальный масштаб *(1 единица = 1 см)*
- **Ориентация модели:**  
  - **Y** — вверх  
  - **Z** — вперёд

